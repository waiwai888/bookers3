<%= render 'users/list', user: current_user, book: @newbook %>
<h1>Books</h1>
<div class="row">
  <table>
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td>
            <%= link_to user_path(book.id) do %>
              <%= attachment_image_tag @user, :user_image, :fill, 60, 60, fallback: "noimage.png" %>
            <% end %>
          </td>
          <td><%= link_to book.title, book_path(book.id) %></td>
          <td><%= book.body %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>