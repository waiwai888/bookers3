<h1>User info</h1>
<table>
  <tbody>
    <tr>
      <td>name</td>
      <td><%= user.name %></td>
    </tr>
    <tr>
      <td>introduction</td>
      <td></td>
    </tr>
  </tbody>
</table>
<div class="row">
  <%= link_to "setting", edit_user_path(current_user) %>
</div>

<h1>New book</h1>
<%= form_with model:book, local:true do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %>

  <%= f.submit '投稿' %>

<% end %>